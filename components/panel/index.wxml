<view class="zan-panel">
  <view
    wx:if="{{ title }}"
    class="zan-panel__title">{{ title }}</view>
  <view
    class="zan-panel__content {{ hideBorder ? 'zan-panel--without-border' : '' }} {{ hideBorderTop ? 'zan-panel--without-border-top' : '' }}"
  >
    <slot></slot>
  </view>
</view>
